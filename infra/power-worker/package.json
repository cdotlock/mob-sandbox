{
  "name": "mob-power-worker",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Worker brokering Vultr power control for mob operators (SSH-signed)",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^3.78.0"
  }
}
